spring.application.name=temp-share
server.port = 8080
spring.datasource.url = jdbc:mysql://localhost:3306/temp_file_share
spring.datasource.username = root
spring.datasource.password = PrajwalPai321

# max size of single upload
spring.servlet.multipart.max-file-size = 10MB

# max size of entire http request
spring.servlet.multipart.max-request-size=10MB

# source folder for storing downloads
file.upload-dir=uploads

spring.jpa.hibernate.ddl-auto = update
spring.jpa.show-sql = true

app.base-url=http://localhost:8080